#######################################################
# This file belongs to the core repository.
# If your project repository is a fork of core,
# you are suggested to keep this file untouched in your project.
# This helps avoid merge conflicts when syncing from core.
#######################################################

name: dummy_nop_large
weight: 1.0  # Sampling weight for weighted dataset mixing
dataset:
  _target_: src.datasets.dummy_nop.dummy_nop.DummyNopDataset
  f_len: 200
  g_len: 100
  fx_dim: 1
  fy_dim: 1
  gx_dim: 1
  gy_dim: 1

num_workers: ${data.num_workers}
pin_memory: ${data.pin_memory}
batch_size_per_device: ${data.batch_size_per_device}
enable_device_seed: ${data.train_enable_device_seed}
base_seed: ${data.train_base_seed}
